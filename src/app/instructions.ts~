import { Component,Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root'
})
export class insistsz {
        ndnM = new Date();
        nd = new Date(this.ndnM.setDate(17));

	prixzday=[{id:1,val:1331}];
	nowadays=[{id:1,val:1331}];
	aftterloadday=[{id:1,val:1331}];

	showsMyMonthsz=false;
	ShowYyears=false;

        SelectingYearofYears=[{id:1,val:1331}];

        Todayn=new Date();
	clicked=this.Todayn.getFullYear().toString()+this.Todayn.getMonth().toString().valid()+this.Todayn.getDate().toString().valid();
        minDayOfTheMonth=29;
        lastDayOfTheMonth=31;
        firstMonthDayOfweek=-1;
        LastMonthId=-1;
        ThisMonthId=-1;
        LastDayLastMonth=-1;
        TodayFullLengthText='Today is '+this.Todayn.myDay()+' '+this.Todayn.myMonth()+' '+this.Todayn.getDate();
	appointments=[];
}
